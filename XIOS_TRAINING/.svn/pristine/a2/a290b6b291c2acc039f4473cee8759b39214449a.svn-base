#!/bin/bash

#For unbuntu VM XIOS is already compiled (XIOS_COMPILED), try to compiling it by yourself in XIOS_TO_COMPILE directory
cd ./XIOS_TO_COMPILE
./make_xios --prod --arch linux_ubuntu --job 4 

# for other systems, adapt the arch files to your architecture or choose an ready one
#  example for IRENE computer

# cd ./XIOS_COMPILED
# ./make_xios --prod --arch X64_IRENE --job 4 

